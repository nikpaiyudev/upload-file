export default function FileUploadHeader() {
    return (
        <div className="fileupload-header">
            <h1>File Upload App</h1>
            <span>This is a app for uploading file to a firebase store</span>
        </div>
    );
}